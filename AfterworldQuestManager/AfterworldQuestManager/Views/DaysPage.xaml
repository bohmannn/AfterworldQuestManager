<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AfterworldQuestManager.Views.DaysPage"
             Title="{Binding Title}"
             xmlns:local="clr-namespace:AfterworldQuestManager.ViewModels"  
             xmlns:model="clr-namespace:AfterworldQuestManager.Models" 
             xmlns:UserControls="clr-namespace:AfterworldQuestManager.UserControls"
             x:Name="BrowseDaysPage">

    <!--
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Add" Command="{Binding AddItemCommand}" />
    </ContentPage.ToolbarItems>
    -->
    
    <ScrollView>
        <StackLayout>
            <UserControls:DaysGridControl Days="{Binding Totals}"/>

        </StackLayout>
    </ScrollView>
   
    
    <!--
      x:DataType enables compiled bindings for better performance and compile time validation of binding expressions.
      https://docs.microsoft.com/xamarin/xamarin-forms/app-fundamentals/data-binding/compiled-bindings
    -->

</ContentPage>
